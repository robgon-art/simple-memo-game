{
  "name": "simple-memo-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "cross-env NODE_OPTIONS=--no-warnings vitest run",
    "test:watch": "cross-env NODE_OPTIONS=--no-warnings vitest",
    "test:coverage": "cross-env NODE_OPTIONS=--no-warnings vitest run --coverage",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "lit": "^3.2.1",
    "three": "^0.175.0"
  },
  "devDependencies": {
    "@open-wc/testing": "^4.0.0",
    "@testing-library/dom": "^9.3.4",
    "@vitest/coverage-v8": "^3.1.1",
    "cross-env": "^7.0.3",
    "gh-pages": "^6.3.0",
    "happy-dom": "^13.6.2",
    "jsdom": "^24.0.0",
    "typescript": "~5.7.2",
    "vite": "^6.2.0",
    "vite-plugin-gh-pages": "^1.0.1",
    "vitest": "^3.1.1"
  }
}
